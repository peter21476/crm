{
  "name": "cms-hubspot-like",
  "version": "1.0.0",
  "description": "HubSpot-like CRM app with React frontend and Express backend",
  "private": true,
  "scripts": {
    "dev": "cd server && npm run dev",
    "dev:all": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "heroku-postbuild": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.x"
  }
}
